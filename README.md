# go-url-shortener 🔗

This is a hobby project, just for fun to help me become more proficient in writing Go(lang) code.

# To-Do List

- [ ] Implement unit tests.
- [ ] Cache the most recently accessed shortened URLs.


## MakeFile

run all make commands with clean tests
```bash
make all build
```

build the application
```bash
make build
```

run the application
```bash
make run
```

live reload the application
```bash
make watch
```

clean up binary from the last build
```bash
make clean
```